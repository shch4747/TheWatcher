"""WA Agent interface (ingestion + chat agent, Spec: "WhatsApp Agent").
Empty in Phase 0 — batch cutting, thread assignment and the chat agent
land in Phases 2, 4 and 5. Declared now so the package boundary exists
from the start (ADR-0003: nothing outside imports below this module).
"""
from __future__ import annotations
