"""Models interface (ADR-0006: three tiers behind one interface). Empty in
Phase 0 — Decision/Worker/Mentor adapters land in Phase 4. Declared now so
other packages can import a stable name.
"""
from __future__ import annotations
